# 🚀 Инструкция по запуску бота

## Предварительные требования

1. **Python 3.10+** (проверьте версию: `python --version`)
2. **Telegram Bot Token** (получите у [@BotFather](https://t.me/BotFather))
3. **Доступ к Marzban Panel** (URL, логин, пароль)
4. **Telegram ID администратора(ов)** (можно узнать у [@userinfobot](https://t.me/userinfobot))

## Пошаговая установка

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

Или если используете виртуальное окружение (рекомендуется):

```bash
# Создать виртуальное окружение
python -m venv venv

# Активировать (Windows)
venv\Scripts\activate

# Активировать (Linux/Mac)
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### 2. Создание файла конфигурации

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# Telegram Bot Configuration
BOT_TOKEN=ваш_токен_бота_от_BotFather
ADMIN_IDS=123456789,987654321

# Marzban API Configuration
MARZBAN_URL=https://ваш-marzban-панель.com
MARZBAN_USERNAME=admin
MARZBAN_PASSWORD=ваш_пароль

# Database Configuration (опционально, по умолчанию SQLite)
DATABASE_URL=sqlite+aiosqlite:///./bot.db

# Trial Settings (опционально)
TRIAL_DATA_LIMIT=5368709120
TRIAL_EXPIRE_DAYS=3

# Logging Configuration (опционально)
LOG_LEVEL=INFO
ENABLE_LOGGING=true
LOG_FILE=bot.log
```

**Важно:**
- Замените `BOT_TOKEN` на токен вашего бота
- Замените `ADMIN_IDS` на ваш Telegram ID (можно несколько через запятую)
- Замените `MARZBAN_URL`, `MARZBAN_USERNAME`, `MARZBAN_PASSWORD` на данные вашей Marzban панели

### 3. Получение Telegram Bot Token

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env` файл

### 4. Получение Telegram ID администратора

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте команду `/start`
3. Бот покажет ваш ID (число, например: `123456789`)
4. Добавьте этот ID в `ADMIN_IDS` в `.env` файле

### 5. Запуск бота

```bash
python main.py
```

Если всё настроено правильно, вы увидите:
```
INFO - Database initialized
INFO - Bot started
```

И получите сообщение в Telegram: "🤖 Бот запущен и готов к работе!"

## Тестирование

### Тестирование пользовательских функций

1. Откройте вашего бота в Telegram
2. Отправьте `/start` - должна появиться клавиатура
3. Нажмите "📊 Моя подписка" - должна показаться информация
4. Нажмите "🎁 Получить тестовый доступ" - должен создаться тестовый пользователь
5. Нажмите "🔗 Подключение" - должна показаться ссылка подписки
6. Нажмите "📱 QR код" - должен сгенерироваться QR код

### Тестирование админских функций

1. Отправьте `/admin` - должна открыться админ панель
2. Нажмите "📊 Статистика" - должна показаться статистика
3. Нажмите "👥 Пользователи" - должен показаться список пользователей
4. Используйте `/user [telegram_id]` для управления конкретным пользователем

## Возможные проблемы и решения

### Ошибка: "BOT_TOKEN is required"
- Проверьте, что файл `.env` существует в корне проекта
- Проверьте, что `BOT_TOKEN` указан в файле `.env`
- Убедитесь, что нет лишних пробелов вокруг `=`

### Ошибка: "Failed to authenticate: 401"
- Проверьте правильность `MARZBAN_URL`, `MARZBAN_USERNAME`, `MARZBAN_PASSWORD`
- Убедитесь, что Marzban панель доступна по указанному URL
- Проверьте права доступа пользователя в Marzban

### Ошибка: "ModuleNotFoundError"
- Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`
- Проверьте, что виртуальное окружение активировано

### Бот не отвечает
- Проверьте, что бот запущен (нет ошибок в консоли)
- Проверьте логи в файле `bot.log`
- Убедитесь, что используете правильный токен бота

### База данных не создается
- Проверьте права на запись в директорию проекта
- Убедитесь, что путь к БД указан правильно
- Проверьте логи на наличие ошибок

## Структура файлов после запуска

После первого запуска должны появиться:
- `bot.db` - база данных SQLite
- `bot.log` - файл логов (если включено логирование)

## Остановка бота

Для остановки бота нажмите `Ctrl+C` в терминале.

## Запуск в фоновом режиме (Linux/Mac)

Для запуска в фоне можно использовать `nohup`:

```bash
nohup python main.py > bot_output.log 2>&1 &
```

Или использовать `screen`:

```bash
screen -S marzban_bot
python main.py
# Нажмите Ctrl+A, затем D для отсоединения
# Для возврата: screen -r marzban_bot
```

## Запуск как служба (Windows)

Можно использовать NSSM (Non-Sucking Service Manager) для запуска как службы Windows.

## Дополнительная информация

- Логи сохраняются в файл `bot.log`
- База данных SQLite находится в файле `bot.db`
- Все настройки можно изменить в файле `.env`
- Планы подписки настраиваются в `config.py`
