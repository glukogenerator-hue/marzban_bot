# 📋 Настройка платежной системы (Telegram Stars)

## Обновление до aiogram 3.7.0

Для поддержки Telegram Stars требуется версия **aiogram 3.7.0** или выше. Обновите зависимости:

```bash
pip install -U aiogram==3.7.0
```

Убедитесь, что в `requirements.txt` указана правильная версия:
```txt
aiogram==3.7.0
```

## Поддерживаемый провайдер

### Telegram Stars
**Рекомендуется для оплаты внутри Telegram (звездами)**

#### Требования:
1. Бот должен быть создан через @BotFather
2. В @BotFather необходимо включить платежи (Menu → Bot Settings → Payments)
3. Выбрать провайдера "Telegram Stars" (если доступно)
4. Получить `BOT_TOKEN` (уже используется для бота)

#### Настройка .env:
```env
TELEGRAM_STARS_ENABLED=true
```
По умолчанию включено, если установлен `BOT_TOKEN`. Для отключения установите `false`.

#### Особенности:
- Оплата производится в звездах (XTR), 1 звезда ≈ 7 RUB
- Сумма автоматически конвертируется из рублей в звезды (округление в большую сторону)
- Платежи обрабатываются внутри Telegram, не требуется переход на внешние сайты
- Используется встроенная в aiogram 3.7.0 поддержка Telegram Stars

## Тарифные планы

| Цена (рубли) | Примерное количество звезд | Срок |
|--------------|----------------------------|------|
| 300₽ | ~43 звезды | 30 дней |
| 750₽ | ~107 звезд | 90 дней |
| 1000₽ | ~143 звезды | 180 дней |
| 2000₽ | ~286 звезд | 365 дней |

*Курс конвертации: 1 звезда ≈ 7 RUB. Фактическое количество звезд может незначительно отличаться в зависимости от текущего курса Telegram.*
*Примечание: Лимит трафика используется только на тестовом периоде. В платных подписках лимит трафика не применяется.*

## Как это работает

### 1. Пользователь нажимает "💳 Купить подписку"
- Бот показывает доступные тарифы в рублях
- Пользователь вводит сумму (одно из стандартных значений: 300, 750, 1000, 2000)

### 2. Создается счет на оплату
- Генерируется уникальный `order_id`
- Создается транзакция в базе данных со статусом "pending"
- Используется метод `answer_invoice` из aiogram 3.7.0 для создания счета
- Пользователь получает кнопку оплаты с указанием количества звезд

### 3. Предпродажная проверка (Pre-checkout)
- Когда пользователь нажимает "Оплатить", Telegram отправляет запрос на предпродажную проверку
- У бота есть **10 секунд** чтобы ответить
- В текущей реализации всегда подтверждаем платеж (`ok=True`)

### 4. Пользователь оплачивает
- Открывается интерфейс оплаты звездами внутри Telegram
- Если звезд недостаточно, система предложит их приобрести
- После успешной оплаты Telegram отправляет уведомление боту

### 5. Обработка успешного платежа
- Бот получает уведомление о successful_payment
- Транзакция обновляется в базе данных (статус "completed")
- Активируется подписка пользователя в Marzban
- Пользователь получает уведомление об успешной оплате

### 6. Команда /paysupport
- Обязательная команда согласно требованиям Telegram
- Предоставляет информацию о возврате средств и контакты поддержки

## Архитектура реализации

### Клавиатура оплаты (`keyboards/payment_keyboard.py`)
```python
def payment_keyboard(amount_stars: int):
    builder = InlineKeyboardBuilder()
    builder.button(text=f"Оплатить {amount_stars} ⭐️", pay=True)
    return builder.as_markup()
```

### Обработчики платежей (`handlers/payment_handlers.py`)
1. **start_payment** - Начало процесса покупки
2. **process_amount** - Обработка выбранной суммы
3. **pre_checkout_handler** - Предпродажная проверка (10 секунд на ответ)
4. **success_payment_handler** - Обработка успешного платежа
5. **pay_support_handler** - Команда /paysupport (обязательная)

### Сервис платежей (`services/payment_service.py`)
- **activate_subscription_after_payment** - Активация подписки после оплаты
- **refund_payment** - Инициация возврата средств
- **check_payment_status** - Проверка статуса платежа

## Регистрация обработчиков в main.py

```python
from handlers.payment_handlers import payment_router

dp = Dispatcher()
dp.include_router(payment_router)
```

Обработчики автоматически регистрируются благодаря декораторам:
- `@payment_router.pre_checkout_query()` - для pre-checkout
- `@payment_router.message(F.successful_payment)` - для успешных платежей

## Безопасность

### Валидация данных
- Все суммы проверяются на соответствие допустимым значениям
- Пользователь должен быть зарегистрирован перед оплатой
- Каждая транзакция сохраняется в базе данных с полной историей

### Защита от повторных списаний
- Каждый платеж имеет уникальный `order_id`
- Статус транзакции проверяется перед активацией подписки
- Предотвращение двойной активации одной и той же транзакции

### Обработка ошибок
- Все ошибки логируются с детальной информацией
- Пользователь получает понятные сообщения об ошибках
- Критические ошибки уведомляют администраторов

## Обработка ошибок

### Распространенные ошибки и решения:

1. **"Платежная система временно недоступна"**
   - Проверьте `TELEGRAM_STARS_ENABLED=true` в .env
   - Убедитесь, что `BOT_TOKEN` корректный

2. **"Не удалось создать транзакцию"**
   - Проверьте подключение к базе данных
   - Убедитесь, что таблицы созданы (`await db_manager.init_db()`)

3. **"Pre-checkout timeout"**
   - У бота есть только 10 секунд на ответ
   - Убедитесь, что сервер отвечает быстро
   - Проверьте логи на наличие ошибок в `pre_checkout_handler`

4. **"Платеж прошел, но подписка не активировалась"**
   - Проверьте подключение к Marzban API
   - Убедитесь, что пользователь существует в Marzban
   - Проверьте логи на ошибки активации

## Мониторинг

### Логи платежей
Все платежи логируются с детальной информацией:
- ID пользователя и транзакции
- Сумма и валюта
- Статус платежа
- Время создания и завершения

### Уведомления администраторам
- При запуске бота все администраторы получают уведомление
- Критические ошибки в платежной системе должны уведомлять администраторов

### Проверка статуса платежей
Пользователи могут:
- Нажать "✅ Проверить оплату" в интерфейсе бота
- Ввести номер заказа вручную (`order_XXXXXXXXXX`)
- Использовать команду "💳 Мои платежи" для просмотра истории

## Возврат средств

### Условия возврата
- Возврат возможен в течение 14 дней с момента оплаты
- Только если услуга не была использована
- Для инициации возврата пользователь должен обратиться к администратору

### Процесс возврата
1. Администратор проверяет возможность возврата
2. Используется метод `refund_star_payment` (если реализован)
3. Транзакция помечается как "refunded"
4. Пользователь уведомляется о возврате

## Тестирование

### Тестовые платежи
Для тестирования используйте:
1. Тестовый режим бота (если доступен)
2. Небольшие суммы для проверки работоспособности
3. Проверку всех этапов: создание счета → pre-checkout → оплата → активация

### Автоматические тесты
Запустите существующие тесты:
```bash
python -m pytest tests/ -v
```

Убедитесь, что все тесты проходят успешно.

## Обновление с предыдущих версий

Если вы обновляетесь с aiogram < 3.7.0:

1. Обновите aiogram до версии 3.7.0
2. Удалите старые реализации платежных провайдеров
3. Убедитесь, что все импорты корректны
4. Протестируйте платежный процесс

## Частые вопросы

### Q: Почему не отображается кнопка оплаты?
**A:** Убедитесь, что:
- Бот имеет разрешение на прием платежей в @BotFather
- Используется aiogram 3.7.0 или выше
- Клавиатура создается с параметром `pay=True`

### Q: Как изменить курс конвертации рублей в звезды?
**A:** Измените функцию `rub_to_stars` в `handlers/payment_handlers.py`:
```python
def rub_to_stars(rub_amount: float) -> int:
    # Измените коэффициент (например, 1 звезда = 6.5 RUB)
    stars = int(rub_amount / 6.5)
    return max(stars, 1)
```

### Q: Как добавить новые тарифные планы?
**A:** Добавьте план в `config.py` в раздел `SUBSCRIPTION_PLANS`:
```python
SUBSCRIPTION_PLANS: dict = {
    "7": {"days": 7, "data_limit": 53687091200, "price": 150},  # 50 GB на 7 дней
    # ... существующие планы
}
```

### Q: Как отключить Telegram Stars?
**A:** Установите в .env:
```env
TELEGRAM_STARS_ENABLED=false
```

### Q: Обязательна ли команда /paysupport?
**A:** Да, согласно требованиям Telegram, бот должен предоставлять информацию о возврате средств через команду `/paysupport`.

## Поддержка

При возникновении проблем:
1. Проверьте логи бота (`bot.log`)
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки в .env файле
4. Обратитесь к документации aiogram 3.7.0

Для вопросов по интеграции Telegram Stars:
- Официальная документация Telegram: https://core.telegram.org/bots/payments
- Документация aiogram: https://docs.aiogram.dev/en/latest/

---

*Последнее обновление: реализация на основе aiogram 3.7.0 с официальной поддержкой Telegram Stars*