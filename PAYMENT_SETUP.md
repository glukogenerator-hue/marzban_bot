# 📋 Настройка платежной системы

## Поддерживаемые провайдеры

### 1. ЮKassa (Yookassa)
**Рекомендуется для рублей (RUB)**

#### Получение данных:
1. Зайдите в [ЮKassa](https://yookassa.ru)
2. Создайте магазин
3. Получите `Shop ID` и `Secret Key` в настройках

#### Настройка .env:
```env
YOOKASSA_SHOP_ID=your_shop_id
YOOKASSA_SECRET_KEY=your_secret_key
```

### 2. CryptoBot (crypt.bot)
**Рекомендуется для криптовалют (USD, USDT, BTC)**

#### Получение данных:
1. Зайдите в [CryptoBot](https://crypt.bot)
2. Создайте API токен
3. Получите `Crypto-Pay-API-Token`

#### Настройка .env:
```env
CRYPTOBOT_TOKEN=your_api_token
```

## Тарифные планы

| Цена | Срок | Лимит трафика |
|------|------|---------------|
| 300₽ | 30 дней | 100 GB |
| 750₽ | 90 дней | 300 GB |
| 1000₽ | 180 дней | 600 GB |
| 2000₽ | 365 дней | 1.2 TB |

## Как это работает

### 1. Пользователь нажимает "💳 Купить подписку"
- Бот показывает доступные тарифы
- Пользователь вводит сумму

### 2. Создается платеж
- Генерируется уникальный `order_id`
- Создается счет у провайдера
- Пользователь получает ссылку на оплату

### 3. Пользователь оплачивает
- Переходит по ссылке
- Оплачивает картой/криптовалютой
- Возвращается в бот

### 4. Проверка оплаты
- Пользователь нажимает "✅ Проверить оплату"
- Бот проверяет статус у провайдера
- При успехе активируется подписка

## Важные моменты

### Безопасность
- Все платежи проходят через официальные API провайдеров
- Бот не хранит данные банковских карт
- Используется HTTPS для всех запросов

### Обработка ошибок
- Автоматические ретраи при ошибках сети
- Circuit breaker для защиты от перегрузки
- Детальное логирование всех операций

### Валидация
- Проверка суммы платежа
- Проверка статуса перед активацией
- Защита от повторных списаний

## Тестирование

### Тестовый режим (ЮKassa)
```python
# В настройках ЮKassa включите тестовый режим
# Используйте тестовые данные карты:
# Номер: 5555 5555 5555 4444
# Срок: 12/30
# CVC: 123
```

### Тестовый режим (CryptoBot)
```python
# CryptoBot автоматически создает тестовые платежи
# в режиме разработчика
```

## Мониторинг

### Логи платежей
```bash
tail -f bot.log | grep "Payment"
```

### База данных
```sql
-- Просмотр всех транзакций
SELECT * FROM transactions ORDER BY created_at DESC;

-- Просмотр успешных платежей
SELECT * FROM transactions WHERE status = 'completed';
```

## Добавление нового провайдера

1. Создайте класс провайдера в `services/payment_service.py`
2. Реализуйте методы `create_invoice` и `check_payment`
3. Добавьте инициализацию в `PaymentService._init_providers`
4. Обновите `.env` с новыми данными

## Частые вопросы

**Q: Почему платеж не подтверждается?**
A: Проверьте, что провайдер настроен правильно и webhook работает

**Q: Можно ли вернуть деньги?**
A: Да, используйте метод `refund` в провайдере

**Q: Как обработать webhook?**
A: Добавьте endpoint в `main.py` для получения уведомлений от провайдера

## Поддержка

Если возникли проблемы:
1. Проверьте логи (`bot.log`)
2. Убедитесь, что API ключи верные
3. Проверьте доступность API провайдера
4. Проверьте настройки вебхуков